## Summary (tasks-9.md)

- **Tasks in this file**: 10
- **Task IDs**: 081 - 090
- **Total Points**: 41

### Main Phase 6-7: Templates, Migration, Multi-Project + UAT Phase 4: Learning

---

## Tasks

### Task ID: 081

- **Title**: TypeScript project template
- **File**: templates/typescript/
- **Complete**: [x]
- **Sprint Points**: 3

- **User Story (business-facing)**: As a technical architect working with TypeScript projects, I want autopilot templates for TypeScript, so that `autopilot init --type typescript` generates appropriate config, quality gates, and agent prompts.
- **Outcome (what this delivers)**: TypeScript project template with ESLint config, tsconfig strict settings, pnpm quality gates, and TypeScript-flavored agent prompts.

#### Prompt:

```markdown
**Objective:** Create the TypeScript project template for scaffolding.

**File to Create/Modify:**
- `templates/typescript/config.yaml.j2`
- `templates/typescript/agents/` (PL, EM, TA, PD prompt templates)
- `templates/typescript/board/` (board files)

**Specification References:**
- Discovery: Package Structure (templates/typescript/)
- Discovery: Quality gates for TypeScript (pnpm lint, typecheck, test)
- RFC Section 6 Phase 5: TypeScript project template

**Prerequisite Requirements:**
1. Task 007 must be complete (Python template as reference)

**Detailed Instructions:**
1. Create `config.yaml.j2` with TypeScript quality gates: pnpm lint --fix, pnpm typecheck, pnpm test, pnpm ci
2. Create agent prompts adapted for TypeScript projects (references to ESLint, TSConfig, pnpm)
3. Create board file templates (same structure as Python)
4. Quality gates use pnpm/npm toolchain instead of uv/ruff/pyright
5. Enforcement rules reference ESLint equivalents

**Acceptance Criteria:**
- [ ] Config template renders valid YAML for TypeScript projects
- [ ] Quality gates use TypeScript toolchain
- [ ] Agent prompts reference TypeScript-specific tools
- [ ] Template renders correctly with Jinja2
```

---

### Task ID: 082

- **Title**: RepEngine layout migration command
- **File**: src/autopilot/cli/migrate.py, src/autopilot/core/migration.py
- **Complete**: [x]
- **Sprint Points**: 5

- **User Story (business-facing)**: As an existing RepEngine user, I want to migrate my autopilot setup to the new .autopilot/ format, so that I can use the new CLI without losing my existing config and history.
- **Outcome (what this delivers)**: The `autopilot migrate` command that converts RepEngine autopilot/ sibling directory layout to .autopilot/ dot-directory format.

#### Prompt:

```markdown
**Objective:** Implement migration from RepEngine autopilot layout.

**File to Create/Modify:**
- `src/autopilot/cli/migrate.py`
- `src/autopilot/core/migration.py`

**Specification References:**
- RFC Section 5: Migration Path from RepEngine Autopilot
- RFC Section 5.2: autopilot migrate command steps
- Discovery: ADR-2 (.autopilot/ vs sibling directory)

**Prerequisite Requirements:**
1. Tasks 002, 006, 010, 012 must be complete (config, db, project init, registry)
2. Write tests in `tests/core/test_migration.py`

**Detailed Instructions:**
1. Implement `MigrationEngine`:
   - `detect_repengine_layout(project_root: Path) -> bool` finds autopilot/ sibling
   - `migrate(project_root: Path) -> MigrationResult` executes 7-step migration
   - Steps per RFC 5.2: detect, read config, copy agents, copy board, convert state to SQLite, register project, generate .gitignore
2. CLI command `autopilot migrate [--project-root PATH]`
3. Non-destructive: original autopilot/ directory is preserved
4. Dry-run mode: `--dry-run` shows what would change
5. State file conversion: usage-tracker.json and hive-sessions.json to SQLite

**Acceptance Criteria:**
- [ ] RepEngine layout is correctly detected
- [ ] Config is mapped to new schema
- [ ] Agent prompts and board files are copied
- [ ] State files are converted to SQLite
- [ ] Original directory is preserved
- [ ] Dry-run mode works
- [ ] All tests pass
```

---

### Task ID: 083

- **Title**: Shell completions for bash, zsh, fish
- **File**: src/autopilot/cli/app.py
- **Complete**: [x]
- **Sprint Points**: 2

- **User Story (business-facing)**: As a CLI user, I want tab completion in my shell, so that I can discover commands and options without memorizing the full reference.
- **Outcome (what this delivers)**: Shell completion scripts generated by Typer for bash, zsh, and fish shells.

#### Prompt:

```markdown
**Objective:** Enable shell completion generation for all supported shells.

**File to Create/Modify:** `src/autopilot/cli/app.py`

**Specification References:**
- Discovery: Shell completions (bash, zsh, fish)
- RFC Section 6 Phase 5: Shell completions

**Prerequisite Requirements:**
1. Task 008 must be complete (CLI app)
2. Write tests in `tests/cli/test_app.py`

**Detailed Instructions:**
1. Add `autopilot --install-completion [SHELL]` using Typer's built-in completion support
2. Add `autopilot --show-completion [SHELL]` to output completion script
3. Verify completions work for all subcommand groups and their options
4. Add completion hints for project names, task IDs, session IDs where applicable
5. Document installation in README

**Acceptance Criteria:**
- [ ] Bash completion script is generated
- [ ] Zsh completion script is generated
- [ ] Fish completion script is generated
- [ ] Subcommand completion works correctly
- [ ] All tests pass
```

---

### Task ID: 084

- **Title**: Global resource broker for multi-project orchestration
- **File**: src/autopilot/orchestration/resource_broker.py
- **Complete**: [x]
- **Sprint Points**: 5

- **User Story (business-facing)**: As a system operator running multiple projects, I want global resource limits, so that concurrent daemons do not exceed machine capacity or Claude Max plan limits.
- **Outcome (what this delivers)**: Global resource broker that enforces max_concurrent_daemons and max_concurrent_agents across all active projects.

#### Prompt:

```markdown
**Objective:** Implement global resource broker for multi-project daemon management.

**File to Create/Modify:** `src/autopilot/orchestration/resource_broker.py`

**Specification References:**
- Discovery: ADR-4 (per-project daemons with global resource limits)
- Discovery: Resource Management (global max_concurrent_daemons, max_concurrent_agents)
- RFC Section 6 Phase 6: Global resource broker

**Prerequisite Requirements:**
1. Tasks 006, 034, 036 must be complete (db, usage, daemon)
2. Write tests in `tests/orchestration/test_resource_broker.py`

**Detailed Instructions:**
1. Implement `ResourceBroker`:
   - `can_start_daemon(project: str) -> tuple[bool, str]` checks global daemon limit
   - `can_spawn_agent(project: str) -> tuple[bool, str]` checks global agent limit
   - `register_daemon(project: str, pid: int)` tracks active daemon
   - `release_daemon(project: str)` removes tracking
   - `get_resource_status() -> ResourceStatus` shows current allocation
2. Limits from ~/.autopilot/config.yaml: max_concurrent_daemons (default 3), max_concurrent_agents (default 6)
3. Per-project priority weights for resource allocation
4. Cleanup: detect dead daemons and release their resources

**Acceptance Criteria:**
- [ ] Daemon count enforced across projects
- [ ] Agent count enforced globally
- [ ] Dead daemon cleanup works
- [ ] Priority weights affect allocation
- [ ] All tests pass
```

---

### Task ID: 085

- **Title**: Per-project usage limits and priority weights
- **File**: src/autopilot/orchestration/usage.py
- **Complete**: [x]
- **Sprint Points**: 3

- **User Story (business-facing)**: As a system operator, I want per-project cycle budgets and priority weights, so that important projects get more cycles and less critical ones are rate-limited.
- **Outcome (what this delivers)**: Per-project usage limit configuration with priority-weighted cycle allocation.

#### Prompt:

```markdown
**Objective:** Add per-project usage limits and priority weighting to usage tracker.

**File to Create/Modify:** `src/autopilot/orchestration/usage.py`

**Specification References:**
- Discovery: Resource Management (per-project limits, priority_weight)
- Discovery: Usage limits configuration (per_project section)

**Prerequisite Requirements:**
1. Task 034 must be complete (usage tracker)
2. Write tests in `tests/orchestration/test_usage.py`

**Detailed Instructions:**
1. Extend UsageTracker with per-project limits:
   - Read per_project overrides from config
   - Priority weight affects cycle allocation in round-robin
   - Higher weight projects get proportionally more cycles
2. Add `allocate_cycles(projects: list, total_budget: int) -> dict[str, int]`
3. Usage reporting: show per-project usage vs limits vs allocation

**Acceptance Criteria:**
- [ ] Per-project limits override global defaults
- [ ] Priority weights affect cycle allocation
- [ ] Usage reporting shows per-project breakdown
- [ ] All tests pass
```

---

### Task ID: 086

- **Title**: Cross-project reporting dashboard
- **File**: src/autopilot/reporting/quality.py, src/autopilot/cli/display.py
- **Complete**: [x]
- **Sprint Points**: 5

- **User Story (business-facing)**: As a technical architect managing multiple projects, I want a cross-project dashboard, so that I can see velocity, quality, and operational health across all my autonomous projects.
- **Outcome (what this delivers)**: Cross-project reporting with aggregated velocity, enforcement quality trends, and operational health metrics.

#### Prompt:

```markdown
**Objective:** Implement cross-project reporting and quality trend analysis.

**File to Create/Modify:**
- `src/autopilot/reporting/quality.py`
- `src/autopilot/cli/display.py`

**Specification References:**
- Discovery: Reporting and Metrics (quality metrics, operational metrics)
- RFC Section 10: Success Metrics
- RFC Section 6 Phase 6: Cross-project reporting dashboard

**Prerequisite Requirements:**
1. Tasks 042, 044, 066 must be complete (velocity, dashboard, enforcement metrics)
2. Write tests in `tests/reporting/test_quality.py`

**Detailed Instructions:**
1. `quality.py`: Implement `QualityReporter`:
   - `enforcement_trends(project_id, days) -> TrendReport` violation density over time
   - `cross_project_summary() -> CrossProjectSummary` all projects aggregated
   - `operational_health(project_id) -> HealthReport` cycle success rate, timeouts, circuit breaker activations
2. Cross-project dashboard panel showing: project name, velocity, quality score, sessions
3. `report summary` command: overall system health
4. `report quality` command: enforcement trend charts
5. `report --export csv` for external analysis

**Acceptance Criteria:**
- [ ] Quality trends show violation density over time
- [ ] Cross-project summary aggregates all projects
- [ ] Operational health metrics are calculated correctly
- [ ] CSV export works
- [ ] All tests pass
```

---

### Task ID: 087

- **Title**: Event-driven scheduler triggers
- **File**: src/autopilot/orchestration/scheduler.py
- **Complete**: [x]
- **Sprint Points**: 5

- **User Story (business-facing)**: As a system operator, I want the scheduler to trigger on events (file changes, git push) not just time intervals, so that cycles run when there is actual work to do.
- **Outcome (what this delivers)**: Event-driven scheduler triggers for file changes (task/board updates) and git pushes, complementing the interval-based approach.

#### Prompt:

```markdown
**Objective:** Add event-driven trigger support to the scheduler.

**File to Create/Modify:** `src/autopilot/orchestration/scheduler.py`

**Specification References:**
- Discovery: Configurable scheduler strategy (interval, event, hybrid)
- Discovery: Scheduler triggers (file_change, git_push, manual)

**Prerequisite Requirements:**
1. Task 035 must be complete (scheduler core)
2. Write tests in `tests/orchestration/test_scheduler.py`

**Detailed Instructions:**
1. Implement trigger system:
   - `FileChangeTrigger`: Watch tasks/ and board/ for changes using filesystem events
   - `GitPushTrigger`: Detect new commits on watched branches
   - `ManualTrigger`: Always available for explicit cycle requests
2. Scheduler strategy from config:
   - "interval": existing behavior (run every N seconds)
   - "event": run only on triggers
   - "hybrid": run on triggers with minimum interval between cycles
3. Debouncing: multiple rapid file changes trigger at most one cycle
4. Config: scheduler.triggers list with type and parameters

**Acceptance Criteria:**
- [ ] File change triggers detect task/board modifications
- [ ] Git push triggers detect new commits
- [ ] Hybrid mode combines interval and event triggers
- [ ] Debouncing prevents rapid-fire cycles
- [ ] All tests pass
```

---

### Task ID: 088

- **Title**: Workflow hooks (pre/post cycle and dispatch)
- **File**: src/autopilot/orchestration/hooks.py
- **Complete**: [x]
- **Sprint Points**: 3

- **User Story (business-facing)**: As a system operator, I want customizable hooks at cycle and dispatch boundaries, so that I can integrate custom checks, notifications, and metrics collection.
- **Outcome (what this delivers)**: Hook system that executes configurable shell commands at pre_cycle, post_cycle, pre_dispatch, and post_dispatch points.

#### Prompt:

```markdown
**Objective:** Implement workflow hooks for cycle and dispatch boundaries.

**File to Create/Modify:** `src/autopilot/orchestration/hooks.py`

**Specification References:**
- Discovery: Workflow Customization (hooks config section)
- Discovery: Hook examples (git fetch, enforce check, metrics record)

**Prerequisite Requirements:**
1. Tasks 004, 035 must be complete (subprocess, scheduler)
2. Write tests in `tests/orchestration/test_hooks.py`

**Detailed Instructions:**
1. Implement `HookRunner`:
   - `run_hooks(hook_point: str, context: dict) -> list[HookResult]`
   - Hook points: pre_cycle, post_cycle, pre_dispatch, post_dispatch
   - Execute each hook command via subprocess
   - Variable substitution in commands: {agent}, {action}, {project}, {cycle_id}
   - Timeout per hook (default 30 seconds)
2. HookResult: command, exit_code, stdout, stderr, duration
3. Hooks loaded from config.hooks section
4. Failure handling: log warning but do not abort cycle (unless hook returns exit code 2)

**Acceptance Criteria:**
- [ ] Hooks execute at correct lifecycle points
- [ ] Variable substitution works in hook commands
- [ ] Hook timeouts are enforced
- [ ] Hook failures are logged but do not block by default
- [ ] All tests pass
```

---

### Task ID: 089

- **Title**: UAT memory integration for test pattern learning
- **File**: src/autopilot/uat/memory.py
- **Complete**: [x]
- **Sprint Points**: 5
- **Spec References**: UAT Discovery: Memory System for Test Pattern Learning, claude-flow memory integration

- **User Story (business-facing)**: As a UAT agent, I want to learn from successful test patterns, so that test generation improves over time and common patterns are reused efficiently.
- **Outcome (what this delivers)**: UAT memory integration using claude-flow memory store/search for test pattern persistence, failure pattern tracking, and false positive detection.

#### Prompt:

```markdown
**Objective:** Implement UAT test pattern learning with claude-flow memory.

**File to Create/Modify:** `src/autopilot/uat/memory.py`

**Specification References:**
- UAT Discovery: Memory System for Test Pattern Learning
- UAT Discovery: claude-flow memory store/search usage
- UAT Discovery: Test templates, failure patterns, false positive patterns

**Prerequisite Requirements:**
1. Tasks 047, 048 must be complete (test generator, test executor)
2. Write tests in `tests/uat/test_memory.py`

**Detailed Instructions:**
1. Implement `UATMemory`:
   - `store_pattern(pattern_name: str, test_type: str, template: str, tags: list[str])`
   - `search_patterns(query: str, limit: int) -> list[TestPattern]`
   - `record_result(task_id: str, result: UATResult)` stores outcomes
   - `get_failure_patterns() -> list[FailurePattern]` common spec mismatches
   - `detect_false_positives(recent_results: list) -> list[str]` tests that always fail incorrectly
2. Use claude-flow memory subprocess: `npx claude-flow memory store/search`
3. Pattern categories: test_templates, failure_patterns, false_positives
4. Namespace: "uat-patterns" for test patterns, "uat-results" for outcomes
5. Test pruning: identify tests that pass 100% for 3+ sprints as removal candidates

**UAT Acceptance Criteria:**
- [ ] Test patterns store and retrieve via claude-flow memory
- [ ] Failure patterns are tracked across runs
- [ ] False positive detection identifies suspicious tests
- [ ] Test pruning candidates are identified
- [ ] All tests pass with mocked claude-flow calls
```

---

### Task ID: 090

- **Title**: UAT hooks intelligence and optimization
- **File**: src/autopilot/uat/optimization.py
- **Complete**: [x]
- **Sprint Points**: 5
- **Spec References**: UAT Discovery: Hooks Intelligence for Improving Test Generation, Phase 4 deliverables

- **User Story (business-facing)**: As a system operator, I want UAT to optimize itself over time, so that test generation quality improves, false positives decrease, and UAT overhead stays minimal.
- **Outcome (what this delivers)**: UAT optimization engine with hooks intelligence integration, test effectiveness metrics, custom test pattern support, and historical trend analysis.

#### Prompt:

```markdown
**Objective:** Implement UAT self-optimization with hooks intelligence.

**File to Create/Modify:** `src/autopilot/uat/optimization.py`

**Specification References:**
- UAT Discovery: Hooks Intelligence for Improving Test Generation
- UAT Discovery: Phase 4 deliverables (pattern learning, false positive tracking, test pruning, spec auto-refresh, custom patterns, exports, trends)

**Prerequisite Requirements:**
1. Tasks 069, 089 must be complete (traceability, memory)
2. Write tests in `tests/uat/test_optimization.py`

**Detailed Instructions:**
1. Implement `UATOptimizer`:
   - `analyze_effectiveness(project_dir) -> EffectivenessReport` tracks which categories catch real bugs
   - `recommend_pruning() -> list[str]` tests that provide no value
   - `suggest_focus_areas() -> list[str]` spec sections with most drift
   - `refresh_spec_index(spec_paths: list[Path])` detect spec changes and rebuild
2. Integration with claude-flow hooks intelligence:
   - Register UAT learning hook pattern
   - Track: test category detection rates, spec drift hotspots, optimal test counts
3. Custom test pattern support: user-defined Jinja2 templates in .autopilot/uat/templates/
4. UAT results export: JSON, CSV, HTML formats
5. Historical trend analysis: UAT pass rate and coverage over sprints

**UAT Acceptance Criteria:**
- [ ] Effectiveness analysis identifies high-value test categories
- [ ] Pruning recommendations are actionable
- [ ] Spec index auto-refresh detects changes
- [ ] Custom test templates load correctly
- [ ] Export works in JSON, CSV, HTML
- [ ] All tests pass
```
